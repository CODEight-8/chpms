version: "3.8"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: chpms
      POSTGRES_USER: chpms_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-chpms_secure_2026}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chpms_user -d chpms"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build: .
    environment:
      DATABASE_URL: postgresql://chpms_user:${DB_PASSWORD:-chpms_secure_2026}@db:5432/chpms
      NEXTAUTH_SECRET: ${NEXTAUTH_SECRET:-chpms-production-secret-change-me}
      NEXTAUTH_URL: http://localhost:3000
      OWNER_EMAIL: ${OWNER_EMAIL:-owner@chpms.local}
      OWNER_PASSWORD: ${OWNER_PASSWORD:-chpms2026}
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

volumes:
  pgdata:
